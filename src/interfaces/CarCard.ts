export interface CarCardInterface {
    image?: string;
    title?: string;
    subtitle?: string;
    trailing?: string;
    available_from?: string;
    available_until?: string;
    onClick?: any;
}